// Distributed under the MIT License.
// See LICENSE.txt for details.

module Main {

  namespace Parallel {

  template <typename Metavariables>
  mainchare [migratable] Main {
    entry Main(CkArgMsg* msg);
    entry void allocate_array_components_and_execute_initialization_phase();
    entry void execute_next_phase();
    entry void register_element_to_checkpoint();

    entry void sync_run_checkpoint();

    entry void restart_from_checkpoint();
  }

  }

}
